STEPS:

-install Node.js and npm

-use npm to install express socketio and pug, you should be able to type npm install in the project directory and it will read the package.json file and just do it


- run : node webserver.js and node gameserver.js in seperate terminals

- open a browser and go to 0.0.0.0:3700